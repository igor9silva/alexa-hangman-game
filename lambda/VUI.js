
// Constants
const {
    WORDS,
    SUPPORTED_LANGUAGES
} = require('./config');

// Utils
const { randomIndex } = require('./util');

module.exports.MESSAGES = {
    'en' : {
        
    },
    'pt': {
        
    }
}

module.exports.WORDS = {
    
}

module.exports.WORDS = {
    'pt': [
        'ventilador',
        'parede',
        'porta',
        'celular',
        'rato',
        'macarrao',
    ],
    'en': [
    ]
}