
// Constants
const {
    WORDS,
    SUPPORTED_LANGUAGES
} = require('./config');

// Utils
const { randomIndex } = require('./util');

const MESSAGES = {
    'en' : {
        
    },
    'pt': {
        
    }
}
